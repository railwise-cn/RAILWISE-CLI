---
title: CLI
description: yonsoon CLI seçenekleri ve komutları.
---

import { Tabs, TabItem } from "@astrojs/starlight/components"

yonsoon CLI herhangi bir argüman olmadan çalıştırıldığında varsayılan olarak [TUI](/docs/tui)'yi başlatır.

```bash
yonsoon
```

Ancak bu sayfada belgelendiği gibi komutları da kabul eder. Bu, yonsoon ile programlı olarak etkileşim kurmanıza olanak tanır.

```bash
yonsoon run "Explain how closures work in JavaScript"
```

---

### tui

yonsoon terminal kullanıcı arayüzünü başlatır.

```bash
yonsoon [project]
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                    |
| ------------ | ---- | --------------------------------------------------------------------------- |
| `--continue` | `-c` | Son oturuma devam et                                                        |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                 |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın) |
| `--prompt`   |      | Kullanılacak prompt                                                         |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                 |
| `--agent`    |      | Kullanılacak agent                                                          |
| `--port`     |      | Dinlenecek port                                                             |
| `--hostname` |      | Dinlenecek host adı                                                         |

---

## Komutlar

yonsoon CLI ayrıca aşağıdaki komutlara da sahiptir.

---

### agent

yonsoon için agent'ları yönetin.

```bash
yonsoon agent [command]
```

---

### attach

`serve` veya `web` komutlarıyla başlatılan, halihazırda çalışan bir yonsoon arka uç sunucusuna bir terminal ekleyin.

```bash
yonsoon attach [url]
```

Bu, TUI öğesinin uzak bir yonsoon arka ucuyla kullanılmasına olanak tanır. Örneğin:

```bash
# Start the backend server for web/mobile access
yonsoon web --port 4096 --hostname 0.0.0.0

# In another terminal, attach the TUI to the running backend
yonsoon attach http://10.20.30.40:4096
```

#### Bayraklar

| Bayrak      | Kısa | Tanım                                |
| ----------- | ---- | ------------------------------------ |
| `--dir`     |      | TUI'yi başlatmak için çalışma dizini |
| `--session` | `-s` | Devam edecek oturum açma bilgileri   |

---

#### create

Özel yapılandırmaya sahip yeni bir agent oluşturun.

```bash
yonsoon agent create
```

Bu komut, özel sistem istemi ve araç yapılandırmasıyla yeni bir agent oluşturma konusunda size yol gösterecektir.

---

#### list

Mevcut tüm agent'ları listeleyin.

```bash
yonsoon agent list
```

---

### auth

Sağlayıcılar için kimlik bilgilerini ve oturum açmayı yönetme komutu.

```bash
yonsoon auth [command]
```

---

#### login

yonsoon, [Models.dev](https://models.dev) adresindeki sağlayıcı listesi tarafından desteklenmektedir, dolayısıyla kullanmak istediğiniz herhangi bir sağlayıcı için API anahtarlarını ayarlamak üzere `yonsoon auth login` kullanabilirsiniz. Bu `~/.local/share/yonsoon/auth.json`'de saklanıyor.

```bash
yonsoon auth login
```

yonsoon başlatıldığında sağlayıcıları kimlik bilgileri dosyasından yükler. Ayrıca ortam değişkenlerindeki veya projenizdeki `.env` dosyasındaki anahtarları da kullanır.

---

#### list

Kimlik bilgileri dosyasında depolanan tüm kimliği doğrulanmış sağlayıcıları listeler.

```bash
yonsoon auth list
```

Veya kısa versiyonu.

```bash
yonsoon auth ls
```

---

#### logout

Bir sağlayıcıyı kimlik bilgileri dosyasından temizleyerek oturumunuzu kapatır.

```bash
yonsoon auth logout
```

---

### github

Depo otomasyonu için GitHub aracısını yönetin.

```bash
yonsoon github [command]
```

---

#### install

GitHub aracısını deponuza yükleyin.

```bash
yonsoon github install
```

Bu komut gerekli GitHub Actions workflow'unu kurar ve yapılandırma adımlarında size rehberlik eder. [Daha fazla bilgi](/docs/github).

---

#### run

GitHub aracısını çalıştırın. Bu genellikle GitHub Eylemlerinde kullanılır.

```bash
yonsoon github run
```

##### Bayraklar

| Bayrak    | Açıklama                                    |
| --------- | ------------------------------------------- |
| `--event` | Aracıyı çalıştırmak için GitHub sahte olayı |
| `--token` | GitHub personal access token                |

---

### mcp

Model Bağlam Protokolü sunucularını yönetin.

```bash
yonsoon mcp [command]
```

---

#### add

Yapılandırmanıza bir MCP sunucusu ekleyin.

```bash
yonsoon mcp add
```

Bu komut, yerel veya uzak bir MCP sunucusu ekleme konusunda size yol gösterecektir.

---

#### list

Yapılandırılmış tüm MCP sunucularını ve bağlantı durumlarını listeleyin.

```bash
yonsoon mcp list
```

Veya kısa versiyonunu kullanın.

```bash
yonsoon mcp ls
```

---

#### auth

OAuth'un etkin olduğu bir MCP sunucusuyla kimlik doğrulaması yapın.

```bash
yonsoon mcp auth [name]
```

Sunucu adı belirtmezseniz mevcut OAuth özellikli sunucular arasından seçim yapmanız istenir.

Ayrıca OAuth özellikli sunucuları ve bunların kimlik doğrulama durumlarını da listeleyebilirsiniz.

```bash
yonsoon mcp auth list
```

Veya kısa versiyonunu kullanın.

```bash
yonsoon mcp auth ls
```

---

#### logout

MCP sunucusu için OAuth kimlik bilgilerini kaldırın.

```bash
yonsoon mcp logout [name]
```

---

#### debug

MCP sunucusu için OAuth bağlantı sorunlarının hatalarını ayıklayın.

```bash
yonsoon mcp debug <name>
```

---

### models

Yapılandırılmış sağlayıcıların tüm mevcut modellerini listeleyin.

```bash
yonsoon models [provider]
```

Bu komut, yapılandırdığınız sağlayıcılardaki modelleri `provider/model` formatında listeler.

Bu, [config dosyanızda](/docs/config/) kullanmanız gereken tam model adını bulmak için kullanışlıdır.

Modelleri bu sağlayıcıya göre filtrelemek için isteğe bağlı olarak bir sağlayıcı kimliğini iletebilirsiniz.

```bash
yonsoon models anthropic
```

#### Bayraklar

| Bayrak      | Tanım                                                                       |
| ----------- | --------------------------------------------------------------------------- |
| `--refresh` | Modeller.dev'den model önbelleğini yenileyin                                |
| `--verbose` | Daha ayrıntılı model çıktısı kullanın (maliyetler gibi meta veriler içerir) |

Önbelleğe alınan model listesini güncellemek için `--refresh` bayrağını kullanın. Bu, bir sağlayıcıya yeni modeller eklendiğinde ve bunları yonsoon'da görmek istediğinizde kullanışlıdır.

```bash
yonsoon models --refresh
```

---

### run

Doğrudan bir istem ileterek yonsoon'u etkileşimli olmayan modda çalıştırın.

```bash
yonsoon run [message..]
```

Bu, komut dosyası oluşturma, otomasyon veya TUI'un tamamını başlatmadan hızlı bir yanıt istediğinizde kullanışlıdır. Örneğin.

```bash "yonsoon run"
yonsoon run Explain the use of context in Go
```

Ayrıca, her çalıştırmada MCP sunucusunun soğuk önyükleme sürelerini önlemek için çalışan bir `yonsoon serve` örneğine de ekleyebilirsiniz:

```bash
# Start a headless server in one terminal
yonsoon serve

# In another terminal, run commands that attach to it
yonsoon run --attach http://localhost:4096 "Explain async/await in JavaScript"
```

#### Bayraklar

| Bayrak       | Kısa | Açıklama                                                                          |
| ------------ | ---- | --------------------------------------------------------------------------------- |
| `--command`  |      | Çalıştırılacak komut, args için mesajı kullanın                                   |
| `--continue` | `-c` | Son oturuma devam et                                                              |
| `--session`  | `-s` | Devam edecek oturum kimliği                                                       |
| `--fork`     |      | Devam ederken oturumu fork'lar (`--continue` veya `--session` ile kullanın)       |
| `--share`    |      | Oturumu paylaşın                                                                  |
| `--model`    | `-m` | provider/model biçiminde kullanılacak model                                       |
| `--agent`    |      | Kullanılacak temsilci                                                             |
| `--file`     | `-f` | Mesaja eklenecek dosya(lar)                                                       |
| `--format`   |      | Biçim: varsayılan (biçimlendirilmiş) veya json (ham JSON olayları)                |
| `--title`    |      | Oturumun başlığı (değer sağlanmazsa kısaltılmış bilgi istemi kullanılır)          |
| `--attach`   |      | Çalışan bir yonsoon sunucusuna ekleyin (ör. http://localhost:4096)               |
| `--port`     |      | Yerel sunucunun bağlantı noktası (varsayılan olarak rastgele bağlantı noktasıdır) |

---

### serve

API erişimi için headless bir yonsoon sunucusu başlatır. Tam HTTP arayüzü için [server docs](/docs/server) sayfasına bakın.

```bash
yonsoon serve
```

Bu, TUI arayüzü olmadan yonsoon işlevselliğine API erişimi sağlayan bir HTTP sunucusunu başlatır. HTTP temel kimlik doğrulamasını etkinleştirmek için `YONSOON_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `yonsoon` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### session

yonsoon oturumlarını yönetin.

```bash
yonsoon session [command]
```

---

#### list

Tüm yonsoon oturumlarını listeleyin.

```bash
yonsoon session list
```

##### Bayraklar

| Bayrak        | Kısa | Tanım                                  |
| ------------- | ---- | -------------------------------------- |
| `--max-count` | `-n` | En son N oturumla sınırla              |
| `--format`    |      | Çıkış formatı: tablo veya json (tablo) |

---

### stats

yonsoon oturumlarınız için belirteç kullanımı ve maliyet istatistiklerini gösterin.

```bash
yonsoon stats
```

#### Bayraklar

| Bayrak      | Açıklama                                                                                                    |
| ----------- | ----------------------------------------------------------------------------------------------------------- |
| `--days`    | Son N güne ait istatistikleri göster (tüm zamanlar)                                                         |
| `--tools`   | Gösterilecek araç sayısı (tümü)                                                                             |
| `--models`  | Model kullanım dökümünü göster (varsayılan olarak gizlidir). En üstteki N'yi göstermek için bir sayı iletin |
| `--project` | Projeye göre filtrele (tüm projeler, boş değer: mevcut proje)                                               |

---

### export

Oturum verilerini JSON olarak dışa aktarın.

```bash
yonsoon export [sessionID]
```

Bir oturum kimliği sağlamazsanız mevcut oturumlar arasından seçim yapmanız istenir.

---

### import

Bir JSON dosyasından veya yonsoon paylaşımından URL oturum verilerini içe aktarın.

```bash
yonsoon import <file>
```

Yerel bir dosyadan veya yonsoon paylaşımından (URL) içe aktarabilirsiniz.

```bash
yonsoon import session.json
yonsoon import https://opncd.ai/s/abc123
```

---

### web

Web arayüzüyle başsız bir yonsoon sunucusu başlatın.

```bash
yonsoon web
```

Bu, bir HTTP sunucusunu başlatır ve bir web arayüzü aracılığıyla yonsoon'a erişmek için bir web tarayıcısı açar. HTTP temel kimlik doğrulamasını etkinleştirmek için `YONSOON_SERVER_PASSWORD` öğesini ayarlayın (kullanıcı adı varsayılan olarak `yonsoon` şeklindedir).

#### Bayraklar

| Bayrak       | Tanım                                      |
| ------------ | ------------------------------------------ |
| `--port`     | Dinlenecek bağlantı noktası                |
| `--hostname` | Dinlenecek ana bilgisayar adı              |
| `--mdns`     | mDNS bulmayı etkinleştir                   |
| `--cors`     | CORS'a izin verecek ek tarayıcı kaynakları |

---

### acp

Bir ACP (Ajan İstemci Protokolü) sunucusu başlatın.

```bash
yonsoon acp
```

Bu komut, nd-JSON kullanarak stdin/stdout aracılığıyla iletişim kuran bir ACP sunucusunu başlatır.

#### Bayraklar

| Bayrak       | Açıklama            |
| ------------ | ------------------- |
| `--cwd`      | Çalışma dizini      |
| `--port`     | Dinlenecek port     |
| `--hostname` | Dinlenecek host adı |

---

### uninstall

yonsoon'u kaldırın ve ilgili tüm dosyaları kaldırın.

```bash
yonsoon uninstall
```

#### Bayraklar

| Bayrak          | Kısa | Tanım                                           |
| --------------- | ---- | ----------------------------------------------- |
| `--keep-config` | `-c` | Yapılandırma dosyalarını sakla                  |
| `--keep-data`   | `-d` | Oturum verilerini ve anlık görüntüleri saklayın |
| `--dry-run`     |      | Nelerin kaldırılmadan kaldırılacağı göster      |
| `--force`       | `-f` | Onay istemlerini atla                           |

---

### upgrade

yonsoon'u en son sürüme veya belirli bir sürüme günceller.

```bash
yonsoon upgrade [target]
```

En son sürüme yükseltmek için.

```bash
yonsoon upgrade
```

Belirli bir sürüme yükseltmek için:

```bash
yonsoon upgrade v0.1.48
```

#### Bayraklar

| Bayrak     | Kısa | Açıklama                                               |
| ---------- | ---- | ------------------------------------------------------ |
| `--method` | `-m` | Kullanılan kurulum yöntemi: curl, npm, pnpm, bun, brew |

---

## Global bayraklar

yonsoon CLI aşağıdaki global bayrakları destekler.

| Bayrak         | Kısa | Tanım                                    |
| -------------- | ---- | ---------------------------------------- |
| `--help`       | `-h` | Yardımı görüntüle                        |
| `--version`    | `-v` | Sürüm numarasını yazdır                  |
| `--print-logs` |      | Günlükleri stderr'e yazdır               |
| `--log-level`  |      | Günlük düzeyi (DEBUG, INFO, WARN, ERROR) |

---

## Ortam değişkenleri

yonsoon ortam değişkenleri kullanılarak yapılandırılabilir.

| Değişken                              | Tip     | Açıklama                                                                    |
| ------------------------------------- | ------- | --------------------------------------------------------------------------- |
| `YONSOON_AUTO_SHARE`                 | boolean | Oturumları otomatik olarak paylaş                                           |
| `YONSOON_GIT_BASH_PATH`              | string  | Windows'ta yürütülebilir Git Bash'in Yolu                                   |
| `YONSOON_CONFIG`                     | string  | Yapılandırma dosyasının yolu                                                |
| `YONSOON_CONFIG_DIR`                 | string  | Yapılandırma dizinine giden yol                                             |
| `YONSOON_CONFIG_CONTENT`             | string  | Satır içi JSON config içeriği                                               |
| `YONSOON_DISABLE_AUTOUPDATE`         | boolean | Otomatik güncelleme kontrollerini devre dışı bırak                          |
| `YONSOON_DISABLE_PRUNE`              | boolean | Eski verilerin temizlenmesini devre dışı bırak                              |
| `YONSOON_DISABLE_TERMINAL_TITLE`     | boolean | Otomatik terminal başlığı güncellemelerini devre dışı bırakın               |
| `YONSOON_PERMISSION`                 | string  | Satır içi JSON izin config'i                                                |
| `YONSOON_DISABLE_DEFAULT_PLUGINS`    | boolean | Varsayılan eklentileri devre dışı bırakın                                   |
| `YONSOON_DISABLE_LSP_DOWNLOAD`       | boolean | Otomatik LSP sunucu indirmelerini devre dışı bırakın                        |
| `YONSOON_ENABLE_EXPERIMENTAL_MODELS` | boolean | Deneysel modelleri etkinleştir                                              |
| `YONSOON_DISABLE_AUTOCOMPACT`        | boolean | Otomatik context sıkıştırmayı devre dışı bırak                              |
| `YONSOON_DISABLE_CLAUDE_CODE`        | boolean | `.claude`'den okumayı devre dışı bırak (istem + beceriler)                  |
| `YONSOON_DISABLE_CLAUDE_CODE_PROMPT` | boolean | `~/.claude/CLAUDE.md` dosyasını okumayı devre dışı bırak                    |
| `YONSOON_DISABLE_CLAUDE_CODE_SKILLS` | boolean | `.claude/skills` yüklemesini devre dışı bırak                               |
| `YONSOON_DISABLE_MODELS_FETCH`       | boolean | Uzak kaynaklardan model getirmeyi devre dışı bırakın                        |
| `YONSOON_FAKE_VCS`                   | string  | Test amaçlı sahte VCS sağlayıcısı                                           |
| `YONSOON_DISABLE_FILETIME_CHECK`     | boolean | Optimizasyon için dosya süresi kontrolünü devre dışı bırakın                |
| `YONSOON_CLIENT`                     | string  | Client kimliği (varsayılan: `cli`)                                          |
| `YONSOON_ENABLE_EXA`                 | boolean | Exa web arama araçlarını etkinleştir                                        |
| `YONSOON_SERVER_PASSWORD`            | string  | `serve`/`web` için temel kimlik doğrulamayı etkinleştirin                   |
| `YONSOON_SERVER_USERNAME`            | string  | Temel kimlik doğrulama kullanıcı adını geçersiz kıl (varsayılan `yonsoon`) |
| `YONSOON_MODELS_URL`                 | string  | Model yapılandırmasını almak için özel URL                                  |

---

### Deneysel

Bu ortam değişkenleri değişebilecek veya kaldırılabilecek deneysel özellikleri etkinleştirir.

| Değişken                                        | Tip     | Tanım                                                   |
| ----------------------------------------------- | ------- | ------------------------------------------------------- |
| `YONSOON_EXPERIMENTAL`                         | boolean | Tüm deneysel özellikleri etkinleştir                    |
| `YONSOON_EXPERIMENTAL_ICON_DISCOVERY`          | boolean | Simge bulmayı etkinleştir                               |
| `YONSOON_EXPERIMENTAL_DISABLE_COPY_ON_SELECT`  | boolean | TUI'da seçim yapıldığında kopyalamayı devre dışı bırak  |
| `YONSOON_EXPERIMENTAL_BASH_DEFAULT_TIMEOUT_MS` | number  | MS cinsinden bash komutları için varsayılan zaman aşımı |
| `YONSOON_EXPERIMENTAL_OUTPUT_TOKEN_MAX`        | number  | LLM yanıtları için maksimum çıktı belirteçleri          |
| `YONSOON_EXPERIMENTAL_FILEWATCHER`             | boolean | Tüm dizin için dosya izleyiciyi etkinleştir             |
| `YONSOON_EXPERIMENTAL_OXFMT`                   | boolean | Oxfmt biçimlendiriciyi etkinleştir                      |
| `YONSOON_EXPERIMENTAL_LSP_TOOL`                | boolean | Deneysel LSP aracını etkinleştir                        |
| `YONSOON_EXPERIMENTAL_DISABLE_FILEWATCHER`     | boolean | Dosya izleyiciyi devre dışı bırak                       |
| `YONSOON_EXPERIMENTAL_EXA`                     | boolean | Deneysel Exa özelliklerini etkinleştirin                |
| `YONSOON_EXPERIMENTAL_LSP_TY`                  | boolean | Deneysel LSP tür denetimini etkinleştir                 |
| `YONSOON_EXPERIMENTAL_MARKDOWN`                | boolean | Deneysel işaretleme özelliklerini etkinleştir           |
| `YONSOON_EXPERIMENTAL_PLAN_MODE`               | boolean | Plan modunu etkinleştir                                 |
