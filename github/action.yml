name: "yonsoon GitHub Action"
description: "Run yonsoon in GitHub Actions workflows"
branding:
  icon: "code"
  color: "orange"

inputs:
  model:
    description: "Model to use"
    required: true

  agent:
    description: "Agent to use. Must be a primary agent. Falls back to default_agent from config or 'build' if not found."
    required: false

  share:
    description: "Share the yonsoon session (defaults to true for public repos)"
    required: false

  prompt:
    description: "Custom prompt to override the default prompt"
    required: false

  use_github_token:
    description: "Use GITHUB_TOKEN directly instead of YONSOON (甬算) App token exchange. When true, skips OIDC and uses the GITHUB_TOKEN env var."
    required: false
    default: "false"

  mentions:
    description: "Comma-separated list of trigger phrases (case-insensitive). Defaults to '/yonsoon,/oc'"
    required: false

  oidc_base_url:
    description: "Base URL for OIDC token exchange API. Only required when running a custom GitHub App install. Defaults to https://api.yonsoon.ai"
    required: false

runs:
  using: "composite"
  steps:
    - name: Get yonsoon version
      id: version
      shell: bash
      run: |
        VERSION=$(curl -sf https://api.github.com/repos/anomalyco/yonsoon/releases/latest | grep -o '"tag_name": *"[^"]*"' | cut -d'"' -f4)
        echo "version=${VERSION:-latest}" >> $GITHUB_OUTPUT

    - name: Cache yonsoon
      id: cache
      uses: actions/cache@v4
      with:
        path: ~/.yonsoon/bin
        key: yonsoon-${{ runner.os }}-${{ runner.arch }}-${{ steps.version.outputs.version }}

    - name: Install yonsoon
      if: steps.cache.outputs.cache-hit != 'true'
      shell: bash
      run: curl -fsSL https://yonsoon.ai/install | bash

    - name: Add yonsoon to PATH
      shell: bash
      run: echo "$HOME/.yonsoon/bin" >> $GITHUB_PATH

    - name: Run yonsoon
      shell: bash
      id: run_yonsoon
      run: yonsoon github run
      env:
        MODEL: ${{ inputs.model }}
        AGENT: ${{ inputs.agent }}
        SHARE: ${{ inputs.share }}
        PROMPT: ${{ inputs.prompt }}
        USE_GITHUB_TOKEN: ${{ inputs.use_github_token }}
        MENTIONS: ${{ inputs.mentions }}
        OIDC_BASE_URL: ${{ inputs.oidc_base_url }}
