---
title: ACP-understøttelse
description: Brug RAILWISE (甬算) i enhver ACP-kompatibel editor.
---

RAILWISE (甬算) understøtter [Agent Client Protocol](https://agentclientprotocol.com) eller (ACP), så du kan bruge det direkte i kompatible editorer og IDE'er.

:::tip
For en liste over editorer og værktøjer, der understøtter ACP, tjek [ACP fremskridtsrapport](https://zed.dev/blog/acp-progress-report#available-now).
:::

ACP er en åben protokol, der standardiserer kommunikation mellem kodeeditorer og AI-kodningsagenter.

---

## Konfiguration

For at bruge RAILWISE (甬算) via ACP, konfigurer din editor til at køre kommandoen `railwise acp`.

Kommandoen starter RAILWISE (甬算) som en ACP-kompatibel underproces, der kommunikerer med din editor over JSON-RPC via stdio.

Nedenfor er eksempler på populære editorer, der understøtter ACP.

---

### Zed

Føj til din [Zed](https://zed.dev)-konfiguration (`~/.config/zed/settings.json`):

```json title="~/.config/zed/settings.json"
{
  "agent_servers": {
    "RAILWISE (甬算)": {
      "command": "railwise",
      "args": ["acp"]
    }
  }
}
```

For at åbne den skal du bruge handlingen `agent: new thread` i **Kommandopaletten**.

Du kan også binde en tastaturgenvej ved at redigere din `keymap.json`:

```json title="keymap.json"
[
  {
    "bindings": {
      "cmd-alt-o": [
        "agent::NewExternalAgentThread",
        {
          "agent": {
            "custom": {
              "name": "RAILWISE (甬算)",
              "command": {
                "command": "railwise",
                "args": ["acp"]
              }
            }
          }
        }
      ]
    }
  }
]
```

---

### JetBrains IDE'er

Tilføj til din [JetBrains IDE](https://www.jetbrains.com/) acp.json i henhold til [dokumentationen](https://www.jetbrains.com/help/ai-assistant/acp.html):

```json title="acp.json"
{
  "agent_servers": {
    "RAILWISE (甬算)": {
      "command": "/absolute/path/bin/railwise",
      "args": ["acp"]
    }
  }
}
```

For at åbne den skal du bruge den nye 'RAILWISE (甬算)'-agent i AI Chat-agentvælgeren.

---

### Avante.nvim

Føj til din [Avante.nvim](https://github.com/yetone/avante.nvim)-konfiguration:

```lua
{
  acp_providers = {
    ["railwise"] = {
      command = "railwise",
      args = { "acp" }
    }
  }
}
```

Hvis du har brug for at videregive miljøvariabler:

```lua {6-8}
{
  acp_providers = {
    ["railwise"] = {
      command = "railwise",
      args = { "acp" },
      env = {
        RAILWISE_API_KEY = os.getenv("RAILWISE_API_KEY")
      }
    }
  }
}
```

---

### CodeCompanion.nvim

For at bruge RAILWISE (甬算) som en ACP-agent i [CodeCompanion.nvim](https://github.com/olimorris/codecompanion.nvim), skal du tilføje følgende til din Neovim-konfiguration:

```lua
require("codecompanion").setup({
  interactions = {
    chat = {
      adapter = {
        name = "railwise",
        model = "claude-sonnet-4",
      },
    },
  },
})
```

Denne konfiguration sætter CodeCompanion til at bruge RAILWISE (甬算) som ACP-agent til chat.

Hvis du har brug for at sende miljøvariabler (som `RAILWISE_API_KEY`), henvises til [Konfiguration af adaptere: Miljøvariabler](https://codecompanion.olimorris.dev/getting-started#setting-an-api-key) i CodeCompanion.nvim-dokumentationen for alle detaljer.

## Understøttelse

RAILWISE (甬算) fungerer på samme måde via ACP som i terminalen. Alle funktioner understøtter:

:::note
Nogle indbyggede slash-kommandoer som `/undo` og `/redo` er i øjeblikket ikke understøttet.
:::

- Indbyggede værktøjer (filoperationer, terminalkommandoer osv.)
- Brugerdefinerede værktøjer og slash-kommandoer
- MCP-servere konfigureret i din RAILWISE (甬算)-konfiguration
- Projektspecifikke regler fra `AGENTS.md`
- Brugerdefinerede formatere og linters
- Agenter og tilladelsessystem
